# Run this project on Mac or Linux
- install pip (Python package manager) 
- install numpy `pip install numpy`
- install scipy `pip install scipy`
- go into project folder and run `python image.py`

# Run on Windows
- Get a Mac or Linux
